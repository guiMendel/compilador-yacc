function factorial(number)
  # Accumulates the result
  var result;
  var result;

  result = 1;

  while (number > 0) do
    result = result * number;
    
    number = number - 1;
  end

  return result;
end

var testNumber;

read testNumber;

print(factorial(testNumber));
