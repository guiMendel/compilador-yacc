function factorial(number) {
  # Accumulates the result
  var result;

  result = 1;

  while (number > 0) {
    result = result * number;
    
    number = number - 1;
  }

  return result;
}

print(factorial(3));
print(factorial(7));
print(factorial(23));