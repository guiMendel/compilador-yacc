function factorial(number) do
  # Accumulates the result
  var result;

  result = 1;

  while (number > 0) do
    result = result * number;
    
    number = number - 1;
  end

  return result;
end

print factorial(3);
print factorial(7);
print factorial(23);