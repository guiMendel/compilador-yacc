function factorial(number) do
  # Accumulates the result
  var result;

  result = 1;

  while (number > 0) do
    result = result * number;
    
    number = number - 1;
  end

  return result;
end

write factorial(3);
write factorial(7);
write factorial(23);